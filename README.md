# Soru–Cevap Mobil (Flutter) — Codemagic Android APK

## Codemagic
- Workflow: **Android APK (Installable)** (android-apk)
- Env var: `API_BASE_URL` (default already set)

This repo intentionally keeps only `lib/` + configs. `android/` is generated in CI via `flutter create` if missing.
